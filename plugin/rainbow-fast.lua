
-- plugin/rainbow_fast.lua
-- This file is sourced automatically by Neovim's plugin loader.
-- It does NOT call setup() — that is intentionally left to the user so they
-- can pass their own config. If you want zero-config auto-setup, uncomment:
--
require("rainbow-fast").setup()

-- Nothing to do here; setup() registers its own autocommands.
